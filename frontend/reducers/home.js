import type { Action } from './types';

const initialState = {
    use_cases: []
};

export default function deviceReducer(state=initialState, action: Action) {

  switch (action.type) {
  }

  return state
}