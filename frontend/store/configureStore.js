// @flow
import configureStoreBase from './configureStoreBase';

const selectedConfigureStore = configureStoreBase;
export const { configureStore } = selectedConfigureStore;
export const { history } = selectedConfigureStore;
